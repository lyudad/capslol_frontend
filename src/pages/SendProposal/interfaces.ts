export interface IProps {
    fs: string;
    color: string;
    mb?: string;
}

export interface IFormValue {
    coverLetter: string;
    freelancerValue: number;
}

export interface IJob {
    id: number;
}
